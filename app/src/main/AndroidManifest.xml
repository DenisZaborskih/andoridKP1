<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.prilozhuha">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prilozhuha"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegActivity"
            />
        <activity android:name=".ClientMActivity" />
        <activity android:name=".TeachMActivity" />
        <activity android:name=".AnketaActivity" />
        <activity android:name=".TeachAnkActivity" />
        <activity android:name=".ClientAnkActivity" />
        <activity android:name=".profileActivity" />
        <activity android:name=".scheduleRegActivity" />
        <activity android:name=".scheduleActivity" />
        <activity android:name=".reviewActivity" />
    </application>

</manifest>